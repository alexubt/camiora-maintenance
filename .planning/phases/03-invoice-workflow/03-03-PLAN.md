---
phase: 03-invoice-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - INV-05
  - INV-06

must_haves:
  truths:
    - "User can select a unit from fleet roster dropdown"
    - "User can pick a date and choose a maintenance type"
    - "Filename preview shows UNIT_DATE_TYPE.pdf before upload"
    - "PDF uploads to correct per-unit OneDrive folder"
    - "Invoice record is appended to invoices.csv after upload"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete invoice workflow end-to-end in the browser.

Purpose: Confirm all INV requirements work together on a real device with real OneDrive data.

Output: Human verification that the invoice workflow is production-ready.
</objective>

<execution_context>
@C:/Users/FleetManager/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FleetManager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-invoice-workflow/03-01-SUMMARY.md
@.planning/phases/03-invoice-workflow/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete invoice workflow in browser</name>
  <files></files>
  <action>
    Present the verification checklist to the user. All automation is complete from Plans 01 and 02. This task confirms everything works end-to-end with live OneDrive data.

    What was built: Complete invoice workflow — unit dropdown from fleet roster, date picker, maintenance type selector, cost field, auto-naming (UNIT_DATE_TYPE.pdf), upload to Fleet Maintenance/UNIT/Invoices/, and invoice record append to invoices.csv.

    Verification steps for the user:
    1. Open the app in a browser (GitHub Pages URL or local server)
    2. Sign in with Microsoft account
    3. INV-01: Verify the unit dropdown populates with fleet units from OneDrive (not hardcoded truck/trailer options). If units show "Loading units..." wait a moment and check they populate.
    4. INV-02: Verify the date field defaults to today's date. Try clearing it — the Upload button should become disabled.
    5. INV-03: Select "Other" from service type — verify a custom text input appears. Type a custom service name. Switch back to a preset — verify the custom field hides.
    6. INV-04: Select a unit, date, and service type. Verify the filename preview shows UNIT_DATE_TYPE.pdf format (e.g., TR-042_2026-03-16_oil-change.pdf). Verify there is NO mileage suffix.
    7. INV-05: Verify the path preview shows OneDrive / Fleet Maintenance / UNIT / Invoices / (no "Trucks" or "Trailers" subfolder).
    8. INV-06: Scan or attach a document and tap Upload. After upload succeeds, check OneDrive:
       a. PDF is in Fleet Maintenance/UNIT/Invoices/ folder
       b. Fleet Maintenance/data/invoices.csv exists and contains a row with InvoiceId, UnitId, Date, Type, Cost, PdfPath
    9. Verify the cost field is present and optional (upload works with cost empty or filled)
    10. Verify form resets after successful upload (date resets to today, service type clears)
  </action>
  <verify>User confirms all 10 verification steps pass by typing "approved"</verify>
  <done>All INV requirements verified in live browser session. No regressions in scanner or OCR.</done>
</task>

</tasks>

<verification>
Human verification of all INV requirements in a live browser session.
</verification>

<success_criteria>
- All 10 verification steps pass
- Invoice appears in correct OneDrive folder
- Invoice record row exists in invoices.csv
- No regressions in scanner or OCR functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-invoice-workflow/03-03-SUMMARY.md`
</output>

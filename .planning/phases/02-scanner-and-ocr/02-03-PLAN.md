---
phase: 02-scanner-and-ocr
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - OCR-01
  - OCR-02
  - OCR-03

must_haves:
  truths:
    - "A photo of an invoice is converted to high-contrast B&W that looks like a scanned document"
    - "A crooked photo is automatically straightened before PDF generation"
    - "Scanning 5+ invoices sequentially does not crash the app on an older iPhone"
    - "OCR extracts text and pre-populates unit, date, and type fields"
    - "User can correct any OCR-detected field before submitting"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete scanner and OCR pipeline works end-to-end in a real mobile browser. This is a human verification checkpoint covering all Phase 2 requirements.

Purpose: Visual and functional quality can only be confirmed by a human using the actual app on a phone.
Output: Confirmation that all Phase 2 success criteria are met.
</objective>

<execution_context>
@C:/Users/FleetManager/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FleetManager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-scanner-and-ocr/02-01-SUMMARY.md
@.planning/phases/02-scanner-and-ocr/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify scanner and OCR pipeline on mobile device</name>
  <files></files>
  <action>
    Present the verification checklist to the user. This is a human-verify checkpoint — no code changes needed.

    Complete scanner pipeline with B&W conversion, deskew, canvas memory management, and OCR auto-fill was built in Plans 01 and 02:
    - Image processing extracted to app/imaging/scanner.js
    - Black-corner bug fixed (coordinate clamping in perspective warp)
    - Rotation deskew added (angle from detected corners)
    - jsPDF upgraded from 2.5.2 to 4.2.0
    - Canvas memory released immediately after blob conversion
    - OCR module (app/imaging/ocr.js) with lazy Tesseract.js loading
    - Form auto-fill from OCR results (only fills empty fields)

    Ask user to test on a phone:

    **Test 1 — B&W Quality (SCAN-01):**
    1. Open the app on a phone
    2. Tap "Scan document" and photograph a printed invoice
    3. Verify the result looks like a high-contrast black-and-white scanned document
    4. Expected: Text is crisp, background is white, no gray areas

    **Test 2 — Deskew (SCAN-02):**
    1. Deliberately hold the phone at a slight angle (5-10 degrees)
    2. Take a photo of a document
    3. Verify the output PDF is straightened
    4. Expected: Text lines appear horizontal in the preview

    **Test 3 — Memory Stability (SCAN-03):**
    1. Scan 5 invoices in a row (tap scan, wait for processing, tap "Add another page" or start new scan)
    2. Verify the app does not crash or become unresponsive
    3. Expected: All 5 scans complete without errors

    **Test 4 — jsPDF Upgrade (SCAN-04):**
    1. Open browser DevTools > Console
    2. Type `window.jspdf.version` or check Network tab for jspdf@4.2.0
    3. Expected: Version 4.2.0 loaded (not 2.5.2)

    **Test 5 — OCR Auto-Fill (OCR-01, OCR-02, OCR-03):**
    1. Scan an invoice that has a visible unit number, date, and service type
    2. Wait a few seconds after scanning (OCR runs in background)
    3. Verify the Unit #, Date, and Service Type fields are pre-populated
    4. Expected: Fields show OCR-detected values; you can edit any field freely
    5. Now type a value into Unit # first, then scan another invoice
    6. Expected: Unit # field is NOT overwritten (OCR respects user input)

    Resume signal: Type "approved" if all tests pass, or describe any issues found.
  </action>
  <verify>Manual verification by user — see test checklist above</verify>
  <done>User confirms all 5 tests pass: B&W quality, deskew, memory stability, jsPDF version, and OCR auto-fill behavior</done>
</task>

</tasks>

<verification>
All verification is manual in this plan — see the checkpoint task above.
</verification>

<success_criteria>
1. Human confirms B&W output looks like a scanned document
2. Human confirms deskew straightens crooked photos
3. Human confirms 5+ sequential scans without crash
4. Human confirms jsPDF 4.2.0 is active
5. Human confirms OCR pre-fills fields correctly and does not overwrite user input
</success_criteria>

<output>
After completion, create `.planning/phases/02-scanner-and-ocr/02-03-SUMMARY.md`
</output>

---
phase: 04-maintenance-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/state.js
  - app/router.js
  - app/main.js
  - app/views/unit-detail.js
  - app/views/unit-detail.test.js
autonomous: true
requirements:
  - FLEET-02
  - FLEET-05
  - FLEET-06

must_haves:
  truths:
    - "User can navigate to #unit?id=TR-042 and see that unit's detail page"
    - "Unit detail page shows invoice history filtered to that unit"
    - "Unit detail page shows PM schedule with overdue items flagged"
    - "Unit detail page shows condition data (mileage, DOT expiry, tire notes)"
    - "Condition data can be updated inline and saved to condition.csv"
    - "A 404 on maintenance.csv or condition.csv renders empty sections, not errors"
  artifacts:
    - path: "app/views/unit-detail.js"
      provides: "Unit detail view with history, schedule, and condition sections"
      exports: ["render"]
      min_lines: 100
    - path: "app/views/unit-detail.test.js"
      provides: "Tests for unit detail data loading and condition update logic"
      min_lines: 40
    - path: "app/router.js"
      provides: "Extended router with query-param support for #unit?id=X"
      exports: ["initRouter"]
    - path: "app/state.js"
      provides: "Extended state with maintenance/condition paths and data"
      exports: ["state"]
    - path: "app/main.js"
      provides: "Extended boot with loadFleetData loading maintenance + condition CSVs"
  key_links:
    - from: "app/router.js"
      to: "app/views/unit-detail.js"
      via: "ROUTES['#unit'] = renderUnitDetail"
      pattern: "ROUTES.*#unit.*renderUnitDetail"
    - from: "app/views/unit-detail.js"
      to: "app/maintenance/schedule.js"
      via: "import { isOverdue, getDueDate, getDueMiles }"
      pattern: "import.*isOverdue.*schedule"
    - from: "app/views/unit-detail.js"
      to: "app/graph/csv.js"
      via: "downloadCSV + parseCSV + serializeCSV + writeCSVWithLock for data loading and condition save"
      pattern: "downloadCSV|writeCSVWithLock"
    - from: "app/views/upload.js"
      to: "app/views/unit-detail.js"
      via: "unit links in upload form linking to #unit?id=X"
      pattern: "href.*#unit\\?id="
---

<objective>
Build the unit detail page and wire it into the app: extend the router for parameterized routes, extend state for maintenance/condition data, create the unit-detail view that shows invoice history + PM schedule + condition data, and add condition edit forms.

Purpose: This is the core of Phase 4 — FLEET-02 (invoice history), FLEET-05 (condition tracking), and FLEET-06 (unit detail page) all converge here. The user navigates to any unit and sees everything about it in one scrollable view.

Output: Working unit detail page accessible via `#unit?id=TR-042` with all three data sections.
</objective>

<execution_context>
@C:/Users/FleetManager/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FleetManager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-maintenance-tracking/04-RESEARCH.md
@.planning/phases/04-maintenance-tracking/04-01-SUMMARY.md

@app/state.js
@app/router.js
@app/main.js
@app/views/upload.js (reference for view pattern, event delegation, toast)
@app/invoice/record.js (reference for DI pattern and csvOps)
@app/graph/csv.js (downloadCSV, parseCSV, serializeCSV, writeCSVWithLock signatures)
</context>

<interfaces>
<!-- From app/graph/csv.js: -->
```javascript
export async function downloadCSV(remotePath, token): Promise<{text: string|null, hash: string|null}>
export function parseCSV(text: string|null): Array<Object>
export function serializeCSV(headers: string[], rows: Array<Object>): string
export async function writeCSVWithLock(remotePath, originalHash, newCSVText, token): Promise<Object>
```

<!-- From app/maintenance/schedule.js (created in Plan 01): -->
```javascript
export function getDueDate(record): string|null    // YYYY-MM-DD or null
export function getDueMiles(record): number|null
export function isOverdue(record, todayStr, currentMiles): boolean
```

<!-- From app/state.js (current): -->
```javascript
export const state = {
  token: null, tokenExp: 0,
  fleet: { units: [], unitsHash: null, unitsPath: '...', invoices: [], invoicesHash: null, invoicesPath: '...' },
  scanPages: [], activeUnitId: null, isUploading: false,
};
```

<!-- maintenance.csv columns: MaintId,UnitId,Type,IntervalDays,IntervalMiles,LastDoneDate,LastDoneMiles,Notes -->
<!-- condition.csv columns: UnitId,CurrentMiles,DotExpiry,TireNotes,LastUpdated -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend state, router, and main.js for unit detail support</name>
  <files>app/state.js, app/router.js, app/main.js</files>
  <action>
    **app/state.js** — Add maintenance and condition fields to `state.fleet`:
    ```
    maintenance: [],
    maintenancePath: 'Fleet Maintenance/data/maintenance.csv',
    maintenanceHash: null,
    condition: [],
    conditionPath: 'Fleet Maintenance/data/condition.csv',
    conditionHash: null,
    ```

    **app/router.js** — Extend to support parameterized routes:
    1. Add import: `import { render as renderUnitDetail } from './views/unit-detail.js';`
    2. Add route: `'#unit': renderUnitDetail` to the ROUTES map
    3. Parse query params from the hash: `const params = Object.fromEntries(new URLSearchParams(hash.includes('?') ? hash.split('?')[1] : ''));`
    4. Pass params as second argument to the render function: `fn(container, params);`
    5. The existing `renderUpload` function already ignores extra arguments, so this is backwards-compatible.

    **app/main.js** — No changes needed to the boot sequence. The unit-detail view loads its own data on-demand when navigated to (not at boot time). This follows the research anti-pattern guidance: "Do not download all three CSVs on every hashchange." The existing `loadFleetData` stays focused on units.csv only.
  </action>
  <verify>
    <automated>node -e "import('./app/router.js').then(() => console.log('router imports ok')).catch(e => { console.error(e.message); process.exit(1); })"</automated>
  </verify>
  <done>state.js has maintenance/condition fields, router handles #unit route with params, main.js unchanged</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit detail view with invoice history, PM schedule, and condition tracking</name>
  <files>app/views/unit-detail.js, app/views/unit-detail.test.js</files>
  <behavior>
    - loadUnitData filters invoices.csv rows to matching UnitId
    - loadUnitData filters maintenance.csv rows to matching UnitId
    - loadUnitData finds single condition.csv row by UnitId (not filter — find)
    - 404 on condition.csv returns null condition (not error)
    - 404 on maintenance.csv returns empty array (not error)
    - saveConditionUpdate mutates existing row in-place (findIndex + assign), not append
    - saveConditionUpdate creates new row when UnitId not found in condition.csv
    - escapeHtml escapes &, <, >, " characters
    - dotStatus returns EXPIRED when DotExpiry < today
    - dotStatus returns warning when DotExpiry within 30 days
    - dotStatus returns ok when DotExpiry > 30 days away
  </behavior>
  <action>
    **Create `app/views/unit-detail.test.js` first (RED):**
    Use node:test with DI pattern (same as record.test.js). Create a `makeMockOps` helper that returns fake `downloadCSV`, `parseCSV`, `serializeCSV`, `writeCSVWithLock`. Test the extractable pure/DI functions:

    1. `loadUnitData(unitId, token, paths, csvOps)` — test that it filters invoices and maintenance by UnitId, finds condition by UnitId, handles 404 (null text) gracefully
    2. `saveConditionUpdate(unitId, updates, token, conditionPath, csvOps)` — test row-update (existing unit) vs. row-create (new unit). Verify it calls writeCSVWithLock with serialized CSV containing the updated row.
    3. `escapeHtml(str)` — test HTML entity escaping
    4. `dotStatus(dotExpiry, todayStr)` — test expired, warning, ok, unknown states

    **Create `app/views/unit-detail.js` (GREEN):**
    Native ES module following the upload.js pattern. Key structure:

    1. **Constants:** `MAINTENANCE_HEADERS`, `CONDITION_HEADERS` arrays for CSV serialization
    2. **Pure functions (exported for testing):** `escapeHtml`, `dotStatus`, `loadUnitData`, `saveConditionUpdate`
    3. **render(container, params):** Main entry point
       - Read `params.id` for unit ID. If missing, show error and back link.
       - Show loading skeleton immediately
       - Call `loadUnitData` with `Promise.allSettled` for 3 CSV sources (invoices, maintenance, condition)
       - Compute overdue status using `isOverdue`, `getDueDate`, `getDueMiles` from schedule.js
       - Render three sections: Invoice History table, PM Schedule table (with overdue badges), Condition card (with inline edit form)
       - Back link: `<a href="#upload">` at the top

    4. **Invoice History section (FLEET-02):**
       - Table with columns: Date, Type, Cost, PDF link
       - Rows sorted by Date descending
       - PDF link opens in new tab: `<a href="https://..." target="_blank">`
       - Empty state: "No invoices recorded yet"

    5. **PM Schedule section (FLEET-03/04):**
       - Table with columns: Type, Interval, Last Done, Due Date, Status
       - Status column shows overdue badge (red) or ok badge (green)
       - DueDate computed at render time (never stored)
       - "Add PM schedule" button opens inline form: Type (select from presets), IntervalDays, IntervalMiles, Notes
       - Save calls appendMaintenanceRecord (download -> parse -> push -> serialize -> writeCSVWithLock)
       - "Mark done today" button per row: updates LastDoneDate to today, LastDoneMiles to current mileage (from condition)
       - Empty state: "No maintenance schedule configured yet"

    6. **Condition section (FLEET-05):**
       - Display: Current mileage, DOT expiry (with status badge from dotStatus), Tire notes
       - "Edit" button toggles inline form with inputs for CurrentMiles, DotExpiry (date input), TireNotes (text input)
       - Save calls `saveConditionUpdate` which does row-update (findIndex + mutate, NOT append)
       - Strip commas from TireNotes and Notes before saving (Pitfall 6)
       - Set LastUpdated to today on save
       - Empty state: "No condition data — tap Edit to add"

    7. **Navigation links from upload.js:**
       - In the unit `<select>` dropdown area of upload.js, add a small link icon next to the selected unit that navigates to `#unit?id=UNITID`. This is a minor addition — add a "View unit" link that appears when a unit is selected.

    **HTML escaping:** All CSV-sourced values rendered via `innerHTML` must go through `escapeHtml()`.

    **Date handling:** Always use `new Date(dateStr + 'T00:00:00')` for YYYY-MM-DD strings to avoid timezone offset (Pitfall 1).

    **Styling:** Use existing CSS variables (--green-dark, --text-2, etc.) from the app. Add minimal inline styles for badges:
    - `.badge-overdue`: red background
    - `.badge-warning`: yellow/amber background
    - `.badge-ok`: green background
    - `.badge-unknown`: gray background
  </action>
  <verify>
    <automated>node --test app/views/unit-detail.test.js</automated>
  </verify>
  <done>Unit detail view renders invoice history, PM schedule with overdue flags, and condition data with inline edit. Tests pass for data loading, condition update (row-update not append), escapeHtml, and dotStatus.</done>
</task>

</tasks>

<verification>
node --test app/maintenance/schedule.test.js app/views/unit-detail.test.js
</verification>

<success_criteria>
- Navigating to `#unit?id=TR-042` renders the unit detail page
- Invoice history shows records from invoices.csv filtered to that unit
- PM schedule shows maintenance records with computed due dates and overdue badges
- Condition card shows mileage, DOT status, and tire notes
- Condition edit form saves via row-update pattern (not append)
- 404 on maintenance.csv or condition.csv renders gracefully with empty-state messages
- All tests pass: `node --test app/maintenance/schedule.test.js app/views/unit-detail.test.js`
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-tracking/04-02-SUMMARY.md`
</output>

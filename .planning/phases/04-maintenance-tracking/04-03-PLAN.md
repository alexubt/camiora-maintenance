---
phase: 04-maintenance-tracking
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false
requirements:
  - FLEET-02
  - FLEET-03
  - FLEET-04
  - FLEET-05
  - FLEET-06

must_haves:
  truths:
    - "Unit detail page loads and displays all three sections in a real browser"
    - "Overdue items are visually distinguishable from non-overdue items"
    - "Condition edit form saves and persists changes"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete maintenance tracking feature works end-to-end in a real browser with live OneDrive data.

Purpose: All Phase 4 requirements (FLEET-02 through FLEET-06) converge on the unit detail page. This checkpoint confirms the feature works visually and functionally before moving to the dashboard phase.

Output: Human-verified confirmation that the unit detail page works correctly.
</objective>

<execution_context>
@C:/Users/FleetManager/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FleetManager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-maintenance-tracking/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify maintenance tracking feature in browser</name>
  <files></files>
  <action>
    Present the verification checklist to the user. This is a human-verify checkpoint — no code changes.

    What was built:
    - Unit detail page at #unit?id=X showing invoice history, PM schedule, and condition data
    - Router extended to handle parameterized hash routes
    - Overdue maintenance items flagged with visual badges
    - Inline condition edit form (mileage, DOT expiry, tire notes)
    - PM schedule with add/mark-done functionality

    How to verify:
    1. Open the app in a browser and sign in
    2. On the upload page, select any unit from the dropdown and click the "View unit" link
    3. Verify the unit detail page loads with the unit ID in the header
    4. Invoice History (FLEET-02): Check that past invoices for this unit appear in a table (if any exist). Verify PDF links open correctly.
    5. PM Schedule (FLEET-03): Tap "Add PM schedule" and configure a maintenance item (e.g., oil-change, 90 days interval). Verify it appears in the schedule table with a computed due date.
    6. Overdue Alerts (FLEET-04): If any PM item has a past due date, verify it shows a red/warning badge. Try adding a PM with a LastDoneDate far in the past to confirm overdue detection.
    7. Condition Data (FLEET-05): Tap "Edit" on the condition card. Enter mileage (e.g., 152300), DOT expiry date, and tire notes. Save and verify the values persist after navigating away and back.
    8. Full View (FLEET-06): Verify all three sections (history, schedule, condition) are visible on one scrollable page.
    9. Navigate back to the upload page using the back link. Verify the upload form still works normally.
  </action>
  <verify>User confirms all verification steps pass by typing "approved"</verify>
  <done>All Phase 4 requirements (FLEET-02 through FLEET-06) verified working in a real browser with live OneDrive data</done>
</task>

</tasks>

<verification>
Human verification of all FLEET-02 through FLEET-06 requirements in a live browser.
</verification>

<success_criteria>
- User confirms unit detail page displays correctly with all three sections
- Overdue badges appear for past-due maintenance items
- Condition data saves and persists
- Navigation between upload and unit detail works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-tracking/04-03-SUMMARY.md`
</output>

---
phase: 07-dashboard-ux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
  - app/views/dashboard.js
  - app/views/unit-detail.js
autonomous: true
requirements: [C9, C10, C11, C12, C13, B5]

must_haves:
  truths:
    - "Unit detail page renders correctly in dark mode with no hardcoded hex colors"
    - "Loading states show animated skeleton placeholders instead of plain text"
    - "Category tab scrollbar is invisible on Android Chrome"
    - "Back links have a chevron icon and tap feedback"
    - "Empty states display icons and call-to-action buttons"
    - "Milestone rows on dashboard cards use red/green colored text instead of emoji"
  artifacts:
    - path: "style.css"
      provides: "CSS classes for status badges, skeletons, back links, empty states, milestone colors"
      contains: ".status-badge"
    - path: "app/views/dashboard.js"
      provides: "CSS-class-based statusBadge, colored milestone rows, skeleton loading, empty state CTAs"
    - path: "app/views/unit-detail.js"
      provides: "CSS-class-based statusBadge, no inline hex colors, skeleton loading, empty state CTAs, chevron back link"
  key_links:
    - from: "app/views/dashboard.js"
      to: "style.css"
      via: "CSS classes for badges and milestones"
      pattern: "status-badge|milestone-"
    - from: "app/views/unit-detail.js"
      to: "style.css"
      via: "CSS classes replacing inline styles"
      pattern: "status-badge|back-link"
---

<objective>
Refactor all visual/CSS issues across the dashboard and unit detail pages: dark mode compliance, loading skeletons, scrollbar fix, back link feedback, empty state CTAs, and color-coded milestones.

Purpose: Establish the CSS foundation that all subsequent plans build on — badges, skeletons, and design token compliance.
Output: style.css with new utility classes, dashboard.js and unit-detail.js with inline styles replaced by CSS classes.
</objective>

<execution_context>
@C:/Users/FleetManager/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FleetManager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@style.css
@app/views/dashboard.js
@app/views/unit-detail.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS classes for badges, skeletons, back links, empty states, milestone colors, and scrollbar fix</name>
  <files>style.css</files>
  <action>
Add the following CSS classes to style.css, all using existing CSS variables (--text, --bg-2, --border, --green-dark, etc.) so they work in both light and dark mode:

**Status badges (C9):**
```css
.status-badge { display:inline-block; padding:2px 8px; border-radius:10px; font-size:12px; font-weight:600; }
.status-badge--overdue { background: #dc3545; color: #fff; }
.status-badge--expired { background: #dc3545; color: #fff; }
.status-badge--due-soon { background: #ffc107; color: #333; }
.status-badge--warning { background: #ffc107; color: #333; }
.status-badge--ok { background: #28a745; color: #fff; }
.status-badge--unknown { background: #6c757d; color: #fff; }
```
Also add dark mode overrides in the existing `@media (prefers-color-scheme: dark)` block:
```css
.status-badge--due-soon, .status-badge--warning { color: #1a1a1a; }
```

**Milestone row colors (B5):**
```css
.milestone-status { font-size: 12px; font-weight: 600; }
.milestone-status--overdue { color: #dc3545; }
.milestone-status--ok { color: #28a745; }
.milestone-status--na { color: var(--text-3); }
```

**Loading skeletons (C10):**
```css
.skeleton { background: var(--bg-3); border-radius: var(--radius); position: relative; overflow: hidden; }
.skeleton::after {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(90deg, transparent 0%, var(--bg-2) 50%, transparent 100%);
  animation: shimmer 1.5s infinite;
}
@keyframes shimmer { 0% { transform: translateX(-100%); } 100% { transform: translateX(100%); } }
.skeleton-card { height: 120px; margin-bottom: 12px; }
.skeleton-row { height: 40px; margin-bottom: 8px; }
.skeleton-bar { height: 20px; width: 60%; margin-bottom: 8px; }
```

**Tab scrollbar hide (C11):**
```css
.dash-tabs { scrollbar-width: none; }
.dash-tabs::-webkit-scrollbar { display: none; }
```

**Back link (C12):**
```css
.back-link {
  display: inline-flex; align-items: center; gap: 4px;
  color: var(--green-dark); text-decoration: none; font-size: 14px;
  padding: 8px 12px 8px 8px; border-radius: 8px;
  transition: background 0.12s, opacity 0.12s;
  -webkit-tap-highlight-color: transparent;
  touch-action: manipulation;
}
.back-link:active { opacity: 0.6; background: var(--bg-3); }
.back-link svg { flex-shrink: 0; }
```

**Empty states (C13):**
```css
.empty-state { text-align: center; padding: 32px 16px; color: var(--text-2); }
.empty-state svg { margin: 0 auto 12px; color: var(--text-3); }
.empty-state p { font-size: 14px; margin-bottom: 12px; }
.empty-state-cta {
  display: inline-block; padding: 8px 20px;
  background: var(--green-dark); color: #fff;
  border: none; border-radius: 8px;
  font-size: 14px; font-weight: 500; cursor: pointer;
  text-decoration: none;
}
.empty-state-cta:active { opacity: 0.85; }
```

**Unit info card (C9 — replace hardcoded bg):**
```css
.unit-info-card {
  background: var(--bg-2); border-radius: 12px;
  padding: 14px 16px; margin: 8px 0 20px;
}
.unit-info-grid {
  display: grid; grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 8px 16px;
}
.unit-info-label { font-size: 11px; color: var(--text-2); text-transform: uppercase; letter-spacing: 0.5px; }
.unit-info-value { font-size: 14px; font-weight: 500; margin-top: 2px; }
.unit-mileage-row {
  margin-top: 10px; display: flex; gap: 8px; align-items: center;
}
.unit-mileage-row input {
  flex: 1; height: 36px; padding: 0 10px;
  border: 1px solid var(--border); border-radius: 8px;
  font-size: 13px; background: var(--bg); color: var(--text);
}
.unit-mileage-row button {
  background: var(--green-dark); color: #fff; border: none;
  padding: 6px 14px; border-radius: 8px; font-size: 13px; cursor: pointer;
}
```

**Milestone table (C9 — replace hardcoded #dee2e6, #eee):**
```css
.milestone-table { width: 100%; border-collapse: collapse; font-size: 13px; }
.milestone-table th { padding: 8px 4px; text-align: left; border-bottom: 2px solid var(--border); }
.milestone-table td { padding: 8px 4px; border-bottom: 1px solid var(--border); }
```

**Notable mentions card (C9):**
```css
.notable-card { margin-top: 16px; background: var(--bg-2); border-radius: 12px; padding: 14px 16px; }
.notable-card textarea {
  width: 100%; padding: 8px; border: 1px solid var(--border);
  border-radius: 8px; font-size: 13px; font-family: inherit;
  background: var(--bg); color: var(--text); box-sizing: border-box;
}
```

**Invoice table (C9):**
```css
.invoice-table { width: 100%; border-collapse: collapse; font-size: 13px; }
.invoice-table th { padding: 8px 4px; text-align: left; border-bottom: 2px solid var(--border); }
.invoice-table td { padding: 8px 4px; border-bottom: 1px solid var(--border); }
```
  </action>
  <verify>
    <automated>grep -c "status-badge" style.css && grep -c "skeleton" style.css && grep -c "back-link" style.css && grep -c "empty-state" style.css && grep -c "milestone-status" style.css && grep -c "scrollbar-width" style.css</automated>
  </verify>
  <done>style.css contains all new CSS classes: status badges (6 variants), skeleton loader with shimmer animation, back-link with tap feedback, empty-state with CTA, milestone color classes, scrollbar-width:none on .dash-tabs, unit-info-card, milestone-table, notable-card, and invoice-table — all using CSS variables for dark mode compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard.js — CSS-class badges, colored milestones, skeleton loading, empty state CTAs</name>
  <files>app/views/dashboard.js</files>
  <action>
Modify dashboard.js to use the new CSS classes from Task 1:

**statusBadge() function (C9):** Replace inline style with CSS class:
```js
function statusBadge(status, label) {
  return `<span class="status-badge status-badge--${escapeHtml(status)}">${escapeHtml(label)}</span>`;
}
```

**Milestone rows on cards (B5):** In the `msRows` mapping inside `renderDashboard`, replace emoji icons with colored text indicators. Change from:
```js
const icon = s.status === 'overdue' ? '...' : ...
```
To use a colored dot/text with CSS class:
```js
const statusCls = s.status === 'overdue' ? 'milestone-status--overdue'
  : s.status === 'ok' ? 'milestone-status--ok' : 'milestone-status--na';
const icon = s.status === 'overdue' ? '!' : s.status === 'ok' ? '&#10003;' : '—';
```
And render the icon inside `<span class="milestone-status ${statusCls}">${icon}</span>` instead of the raw emoji span.

**Loading skeleton (C10):** Replace the "Loading fleet data..." div with skeleton placeholders:
```html
<div style="padding:16px;">
  <div class="skeleton skeleton-bar"></div>
  <div class="skeleton skeleton-card"></div>
  <div class="skeleton skeleton-card"></div>
</div>
```

**Empty state CTA (C13):** Replace the plain empty state div with:
```html
<div class="empty-state">
  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
    <path d="M3 17L8 7H16L21 17" stroke-linecap="round" stroke-linejoin="round"/>
    <circle cx="5.5" cy="18.5" r="2"/><circle cx="18.5" cy="18.5" r="2"/>
  </svg>
  <p>No ${_activeTab ? _activeTab.toLowerCase() + 's' : 'units'} in the fleet yet.</p>
  <button class="empty-state-cta" id="showAddUnitBtn">Add your first ${_activeTab ? _activeTab.toLowerCase() : 'unit'}</button>
</div>
```
When this CTA is clicked, it should show the add unit form (same behavior as the current "+ Add unit" button). Wire the click to reveal the addUnitForm. Keep the existing "+ Add unit" button below the grid for non-empty states, but when the empty state CTA is shown, hide the separate add button section.

**"All caught up" message (used by B7 in Plan 2):** No changes needed here yet — Plan 2 will handle tab-filtering of action items.
  </action>
  <verify>
    <automated>grep -c "status-badge" app/views/dashboard.js && grep -c "milestone-status" app/views/dashboard.js && grep -c "skeleton" app/views/dashboard.js && grep -c "empty-state" app/views/dashboard.js</automated>
  </verify>
  <done>dashboard.js uses CSS classes for all badges (no inline background styles), milestone rows use colored text instead of emoji, loading shows skeleton placeholders, empty tabs show icon + CTA button.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor unit-detail.js — CSS classes, skeleton loading, back link chevron, empty state CTAs</name>
  <files>app/views/unit-detail.js</files>
  <action>
Modify unit-detail.js to use the new CSS classes from Task 1:

**statusBadge() function (C9):** Replace inline style object with CSS class (same pattern as dashboard.js):
```js
function statusBadge(status, label) {
  return `<span class="status-badge status-badge--${escapeHtml(status)}">${escapeHtml(label || status)}</span>`;
}
```

**Back link (C12):** Replace the plain `<a href="#dashboard">&#8592; Back</a>` with a styled back link using the CSS class:
```html
<a href="#dashboard" class="back-link">
  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M19 12H5M12 19l-7-7 7-7"/>
  </svg>
  Dashboard
</a>
```
Do the same for the back link in the initial loading state and error state.

**Loading skeleton (C10):** Replace "Loading unit data..." text with skeleton shapes:
```html
<div style="padding:16px;">
  <a href="#dashboard" class="back-link">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M19 12H5M12 19l-7-7 7-7"/>
    </svg>
    Dashboard
  </a>
  <h2 style="margin:12px 0 8px;">${escapeHtml(unitId)}</h2>
  <div class="skeleton skeleton-card"></div>
  <div class="skeleton skeleton-bar" style="margin-top:16px;"></div>
  <div class="skeleton skeleton-row"></div>
  <div class="skeleton skeleton-row"></div>
  <div class="skeleton skeleton-row"></div>
</div>
```

**Milestone table (C9):** In `renderUnitPage`, replace `<table style="width:100%;border-collapse:collapse;font-size:13px;">` and all inline border styles on `<tr>` and `<th>` / `<td>` with the CSS class `milestone-table`. Remove all `style="border-bottom:2px solid #dee2e6;..."` and `style="border-bottom:1px solid #eee;"` from the milestone table rows.

**Unit info card (C9):** In `renderUnitInfo`, replace the inline-styled wrapper `<div style="background:var(--bg-2,#f8f9fa);border-radius:12px;padding:14px 16px;margin:8px 0 20px;">` with `<div class="unit-info-card">`. Replace the grid div with `<div class="unit-info-grid">`. Replace the mileage input row inline styles with `<div class="unit-mileage-row">` and remove inline styles from the input and button inside it. Remove `style="border:1px solid #ddd"` from the mileage input — use the CSS class.

**Notable mentions card (C9):** Replace `<div style="margin-top:16px;background:var(--bg-2, #f8f9fa);border-radius:12px;padding:14px 16px;">` with `<div class="notable-card">`. Replace inline-styled textarea with class `notable-card textarea`.

**Invoice table (C9):** Replace inline table styles with `class="invoice-table"`. Remove inline border styles from `<tr>`, `<th>`, `<td>`.

**Invoice empty state (C13):** Replace `<p style="color:var(--text-2);">No invoices recorded yet</p>` with:
```html
<div class="empty-state">
  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
    <path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/>
    <polyline points="14 2 14 8 20 8"/>
  </svg>
  <p>No invoices recorded yet.</p>
  <a href="#upload" class="empty-state-cta">Upload an invoice</a>
</div>
```

**Tire monitor cards (C9):** In `renderTireMonitor`, replace `style="background:var(--bg-2, #f8f9fa);border-radius:12px;padding:12px 14px;"` with `class="notable-card"` (reuse the same card style) or add `style="background:var(--bg-2);..."` — the key fix is removing the hardcoded fallback `#f8f9fa` since `--bg-2` already has dark mode value. Simply change `var(--bg-2, #f8f9fa)` to `var(--bg-2)` everywhere in this file since the variable is always defined.

**General cleanup (C9):** Search for any remaining hardcoded hex colors used in `style=""` attributes (#dee2e6, #eee, #ddd, #dc3545, #28a745, #ffc107, #6c757d, #999, #666). Replace:
- `#ddd` and `#eee` borders with `var(--border)`
- `#999` and `#666` text colors with `var(--text-2)` or `var(--text-3)`
- Badge/button colors (#dc3545 etc.) are handled by CSS classes above
- Keep `#dc3545` only in error message text where it is a one-off error color
  </action>
  <verify>
    <automated>grep -c "status-badge" app/views/unit-detail.js && grep -c "back-link" app/views/unit-detail.js && grep -c "skeleton" app/views/unit-detail.js && grep -c "empty-state" app/views/unit-detail.js && grep -c "milestone-table" app/views/unit-detail.js</automated>
  </verify>
  <done>unit-detail.js uses CSS classes for all badges, milestone table, unit info card, notable card, invoice table. No hardcoded hex colors in style="" attributes (except error messages). Back links have chevron SVG + tap feedback. Loading shows skeleton. Empty invoice state has icon + CTA link to #upload.</done>
</task>

</tasks>

<verification>
1. Open app in Chrome DevTools with dark mode forced — unit detail page should have no white/light backgrounds or unreadable text
2. Grep for hardcoded hex in style attributes: `grep -n 'style=".*#[0-9a-fA-F]' app/views/unit-detail.js app/views/dashboard.js` should return minimal results (only error messages)
3. Dashboard loading state shows shimmer animation
4. Unit detail loading state shows shimmer animation
5. Category tabs on Android have no visible scrollbar
6. Back link on unit detail has chevron icon and background flash on tap
7. Empty dashboard tab shows truck icon + "Add your first truck" CTA
8. Empty invoice section shows document icon + "Upload an invoice" link
9. Dashboard card milestone rows show colored checkmark/exclamation instead of emoji
</verification>

<success_criteria>
- Zero hardcoded hex colors in style="" attributes on unit-detail.js (except error text)
- statusBadge() in both files uses CSS classes, not inline background styles
- Loading states render skeleton shapes with shimmer animation
- .dash-tabs has scrollbar-width:none
- Back links render with chevron SVG and .back-link class
- Empty states render with SVG icon and .empty-state-cta button/link
- Milestone rows on dashboard cards use .milestone-status--overdue (red) / --ok (green) classes
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-ux-improvements/07-01-SUMMARY.md`
</output>

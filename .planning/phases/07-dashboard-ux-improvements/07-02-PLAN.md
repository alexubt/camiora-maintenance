---
phase: 07-dashboard-ux-improvements
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/views/dashboard.js
autonomous: true
requirements: [B4, B6, B7, D16]

must_haves:
  truths:
    - "Fleet summary bar shows total units, overdue count, and due-soon count above the category tabs"
    - "Search bar filters unit cards by Unit ID (case-insensitive substring match)"
    - "Status filter dropdown filters by All / Overdue / Due Soon / OK"
    - "Action item banners filter to match the active category tab"
    - "'All caught up' reflects the filtered (tab-specific) view"
    - "Each dashboard card has a mileage input for quick update without navigating to unit detail"
  artifacts:
    - path: "app/views/dashboard.js"
      provides: "Summary bar, search, status filter, tab-filtered action items, quick mileage input"
      exports: ["render"]
  key_links:
    - from: "app/views/dashboard.js"
      to: "app/views/unit-detail.js"
      via: "saveConditionUpdate import for quick mileage"
      pattern: "import.*saveConditionUpdate"
    - from: "app/views/dashboard.js"
      to: "style.css"
      via: "CSS classes from Plan 01"
      pattern: "status-badge|empty-state"
---

<objective>
Add dashboard UX features: fleet summary bar, search/filter, tab-filtered action items, and quick mileage update on unit cards.

Purpose: Make the dashboard actionable — see fleet health at a glance, find units quickly, and update mileage without drilling into detail pages.
Output: Enhanced dashboard.js with summary bar, search, filters, and inline mileage input.
</objective>

<execution_context>
@C:/Users/FleetManager/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FleetManager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-ux-improvements/07-01-SUMMARY.md
@app/views/dashboard.js
@app/views/unit-detail.js
@app/state.js

<interfaces>
<!-- From app/views/unit-detail.js — needed for quick mileage update -->
```javascript
export async function saveConditionUpdate(unitId, updates, token, conditionPath, csvOps = defaultCsvOps);
// updates: { CurrentMiles, DotExpiry, TireNotes }
// Row-update if exists, row-create if new. Writes to condition.csv.
```

<!-- From app/state.js -->
```javascript
export const state = {
  token: null,
  fleet: {
    units: [],          // Array of { UnitId, Type, VIN, PlateNr, Make, Model, Year, DotExpiry }
    conditionPath: 'Fleet Maintenance/data/condition.csv',
    // ...
  }
};
```

<!-- From app/graph/auth.js -->
```javascript
export function getValidToken(); // Returns Promise<string> — refreshes if needed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fleet summary bar, search/filter, and tab-filtered action items</name>
  <files>app/views/dashboard.js</files>
  <action>
Modify `renderDashboard()` in dashboard.js:

**Fleet summary bar (B4):**
After the header/nav and before action items, add a summary strip:
```html
<div style="display:flex;gap:12px;margin-bottom:12px;">
  <div style="flex:1;background:var(--bg-2);border-radius:var(--radius);padding:10px 14px;text-align:center;">
    <div style="font-size:20px;font-weight:700;">${units.length}</div>
    <div style="font-size:11px;color:var(--text-2);text-transform:uppercase;">Units</div>
  </div>
  <div style="flex:1;background:var(--bg-2);border-radius:var(--radius);padding:10px 14px;text-align:center;">
    <div style="font-size:20px;font-weight:700;color:#dc3545;">${overdueItems.length}</div>
    <div style="font-size:11px;color:var(--text-2);text-transform:uppercase;">Overdue</div>
  </div>
  <div style="flex:1;background:var(--bg-2);border-radius:var(--radius);padding:10px 14px;text-align:center;">
    <div style="font-size:20px;font-weight:700;color:#ffc107;">${dueSoonItems.length}</div>
    <div style="font-size:11px;color:var(--text-2);text-transform:uppercase;">Due Soon</div>
  </div>
</div>
```
This counts across ALL categories (not filtered by active tab), per CONTEXT.md decision.

**Search and status filter (B6):**
Add a module-level variable `let _searchQuery = '';` and `let _statusFilter = 'all';` alongside the existing `_activeTab`.

Below the summary bar and above the action items, render:
```html
<div style="display:flex;gap:8px;margin-bottom:12px;">
  <input type="text" id="dashSearch" placeholder="Search by Unit ID..." value="${escapeHtml(_searchQuery)}"
    style="flex:1;height:40px;padding:0 12px;border:1px solid var(--border);border-radius:var(--radius);font-size:14px;background:var(--bg);color:var(--text);">
  <select id="dashStatusFilter" style="height:40px;padding:0 10px;border:1px solid var(--border);border-radius:var(--radius);font-size:13px;background:var(--bg);color:var(--text);">
    <option value="all"${_statusFilter === 'all' ? ' selected' : ''}>All</option>
    <option value="overdue"${_statusFilter === 'overdue' ? ' selected' : ''}>Overdue</option>
    <option value="due-soon"${_statusFilter === 'due-soon' ? ' selected' : ''}>Due Soon</option>
    <option value="ok"${_statusFilter === 'ok' ? ' selected' : ''}>OK</option>
  </select>
</div>
```

Wire event listeners at the bottom of `renderDashboard`:
- `#dashSearch` on `input` event: set `_searchQuery = e.target.value;` then re-call `renderDashboard(container, allMaintenance, allCondition)`.
- `#dashStatusFilter` on `change` event: set `_statusFilter = e.target.value;` then re-call `renderDashboard(container, allMaintenance, allCondition)`.

**Filter units by search and status (B6):**
After filtering `tabUnits` by active tab category, apply additional filters:
```js
let filteredUnits = tabUnits;
if (_searchQuery) {
  const q = _searchQuery.toLowerCase();
  filteredUnits = filteredUnits.filter(u => u.UnitId.toLowerCase().includes(q));
}
if (_statusFilter !== 'all') {
  filteredUnits = filteredUnits.filter(u => (unitStatusMap[u.UnitId] || 'ok') === _statusFilter);
}
```
Use `filteredUnits` instead of `tabUnits` for rendering cards. Keep the sort logic.

After re-render, restore focus to the search input if it was focused (use `document.getElementById('dashSearch')?.focus()` and set selectionStart to end of value to prevent cursor jump).

**Filter action items by tab (B7):**
Currently `overdueItems` and `dueSoonItems` are built from ALL maintenance records. The action items section should filter by active tab. After building the full lists, create filtered versions:
```js
const tabUnitIds = new Set(units.filter(u => (u.Type || 'Other').trim() === _activeTab).map(u => u.UnitId));
const tabOverdue = overdueItems.filter(item => tabUnitIds.has(item.unitId));
const tabDueSoon = dueSoonItems.filter(item => tabUnitIds.has(item.unitId));
```
Use `tabOverdue` and `tabDueSoon` for rendering the action items HTML. Use the full (unfiltered) counts for the summary bar (B4 says "visible on all tabs, not filtered by active category").

Update the "All caught up" message to reflect the filtered view — show it when `tabOverdue.length === 0 && tabDueSoon.length === 0`.
  </action>
  <verify>
    <automated>grep -c "dashSearch" app/views/dashboard.js && grep -c "dashStatusFilter" app/views/dashboard.js && grep -c "_searchQuery" app/views/dashboard.js && grep -c "tabOverdue" app/views/dashboard.js</automated>
  </verify>
  <done>Summary bar renders with total units / overdue / due-soon counts (unfiltered). Search input filters cards by Unit ID substring. Status dropdown filters by overdue/due-soon/ok. Action items filter by active category tab. "All caught up" reflects the tab-filtered view.</done>
</task>

<task type="auto">
  <name>Task 2: Add quick mileage update input on dashboard cards</name>
  <files>app/views/dashboard.js</files>
  <action>
**Import saveConditionUpdate (D16):**
Add to the import section at the top of dashboard.js:
```js
import { saveConditionUpdate } from './unit-detail.js';
```

**Quick mileage input on cards (D16):**
In the card HTML template (inside the `cardsHtml` mapping), add a mileage input row below the milestone rows, inside the card but stopping event propagation so it doesn't navigate:
```html
<div style="border-top:1px solid var(--border);padding-top:6px;margin-top:6px;" onclick="event.preventDefault();event.stopPropagation();">
  <div style="display:flex;align-items:center;gap:6px;">
    <input type="text" inputmode="numeric" placeholder="${currentMiles ? currentMiles.toLocaleString() : 'Miles'}"
      data-mileage-unit="${escapeHtml(u.UnitId)}"
      style="flex:1;height:32px;padding:0 8px;border:1px solid var(--border);border-radius:8px;font-size:12px;background:var(--bg);color:var(--text);">
    <button data-action="quick-mileage" data-unit-id="${escapeHtml(u.UnitId)}"
      style="height:32px;padding:0 10px;background:var(--green-dark);color:#fff;border:none;border-radius:8px;font-size:12px;cursor:pointer;white-space:nowrap;">Update mi</button>
  </div>
</div>
```

Note: The card is an `<a>` tag. The mileage row uses `onclick="event.preventDefault();event.stopPropagation();"` on its wrapper to prevent the card link navigation when interacting with the mileage input. This is the simplest approach for inline forms inside anchor tags.

**Wire quick mileage handler:**
At the end of `renderDashboard`, add an event listener via delegation on the container:
```js
container.querySelectorAll('[data-action="quick-mileage"]').forEach(btn => {
  btn.addEventListener('click', async (e) => {
    e.preventDefault();
    e.stopPropagation();
    const unitId = btn.dataset.unitId;
    const input = container.querySelector(`[data-mileage-unit="${unitId}"]`);
    const miles = (input?.value || '').trim().replace(/,/g, '');
    if (!miles || isNaN(Number(miles))) return;

    btn.disabled = true;
    btn.textContent = '...';
    try {
      const token = await getValidToken();
      await saveConditionUpdate(unitId, { CurrentMiles: miles }, token, state.fleet.conditionPath);
      // Update local condition map and re-render
      input.value = '';
      input.placeholder = Number(miles).toLocaleString();
      btn.textContent = 'Done!';
      setTimeout(() => { btn.textContent = 'Update mi'; btn.disabled = false; }, 1500);
    } catch (err) {
      console.error('Quick mileage update failed:', err);
      btn.textContent = 'Failed';
      setTimeout(() => { btn.textContent = 'Update mi'; btn.disabled = false; }, 2000);
    }
  });
});
```

Also add click listeners on the mileage inputs to prevent card navigation:
```js
container.querySelectorAll('[data-mileage-unit]').forEach(input => {
  input.addEventListener('click', e => { e.preventDefault(); e.stopPropagation(); });
});
```
  </action>
  <verify>
    <automated>grep -c "saveConditionUpdate" app/views/dashboard.js && grep -c "quick-mileage" app/views/dashboard.js && grep -c "data-mileage-unit" app/views/dashboard.js</automated>
  </verify>
  <done>Each dashboard unit card has a mileage input field and "Update mi" button. Clicking the input/button does not navigate to unit detail. Submitting a mileage value calls saveConditionUpdate to write to condition.csv. Button shows feedback (Done!/Failed) after save.</done>
</task>

</tasks>

<verification>
1. Dashboard renders summary bar with 3 stat boxes (total units, overdue count, due-soon count) above tabs
2. Search input filters cards in real-time by Unit ID (case-insensitive)
3. Status dropdown filters cards by overdue / due-soon / ok
4. Switching category tab updates action items to show only that tab's units
5. "All caught up" message appears only when the active tab has no overdue/due-soon items
6. Mileage input on card does not trigger card navigation
7. Entering mileage and clicking "Update mi" saves to condition.csv and shows feedback
</verification>

<success_criteria>
- Summary bar shows accurate counts from all-categories data
- Search filters by UnitId substring (case-insensitive)
- Status filter narrows cards to matching status
- Action items section shows only items for the active category tab
- Quick mileage input saves to condition.csv via saveConditionUpdate
- No navigation side-effects from mileage input interaction
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-ux-improvements/07-02-SUMMARY.md`
</output>

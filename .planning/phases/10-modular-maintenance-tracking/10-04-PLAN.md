---
phase: 10-modular-maintenance-tracking
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - app/views/unit-detail.js
  - sw.js
autonomous: false
requirements:
  - MAINT-11
  - MAINT-12

must_haves:
  truths:
    - "Trailer unit detail page shows tire positions with axle grouping (not steer/drive)"
    - "Service worker cache includes settings.js"
    - "Full settings CRUD flow works end-to-end in browser"
    - "Dashboard collapse/expand and Coming Due tab work in browser"
  artifacts:
    - path: "sw.js"
      provides: "Cache version bumped, settings.js in STATIC array"
      contains: "settings.js"
  key_links:
    - from: "sw.js"
      to: "app/views/settings.js"
      via: "STATIC array includes './app/views/settings.js'"
      pattern: "settings\\.js"
---

<objective>
Verify trailer tire positions in unit detail, update service worker cache with settings.js, and perform full browser verification of all Phase 10 features.

Purpose: Final integration — ensures trailer tire monitor is correct, new settings.js is cached for offline use, and all features work end-to-end.
Output: Updated sw.js, verified unit-detail.js trailer tire rendering, full feature verification.
</objective>

<execution_context>
@C:/Users/FleetManager/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FleetManager/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-modular-maintenance-tracking/10-CONTEXT.md
@.planning/phases/10-modular-maintenance-tracking/10-01-SUMMARY.md
@.planning/phases/10-modular-maintenance-tracking/10-02-SUMMARY.md
@.planning/phases/10-modular-maintenance-tracking/10-03-SUMMARY.md

<interfaces>
From app/maintenance/milestones.js (TIRE_POSITIONS):
```javascript
export const TIRE_POSITIONS = [
  { key: 'steer-l', label: 'Steer L' },
  { key: 'steer-r', label: 'Steer R' },
  { key: 'drive-outer-l', label: 'Drive Outer L' },
  { key: 'drive-outer-r', label: 'Drive Outer R' },
  { key: 'drive-inner-l', label: 'Drive Inner L' },
  { key: 'drive-inner-r', label: 'Drive Inner R' },
  { key: 'trailer-1-l', label: 'Trailer 1 L' },
  { key: 'trailer-1-r', label: 'Trailer 1 R' },
  { key: 'trailer-2-l', label: 'Trailer 2 L' },
  { key: 'trailer-2-r', label: 'Trailer 2 R' },
];
```

From sw.js (current cache):
```javascript
const CACHE = 'camiora-v51';
const STATIC = [
  // ... existing entries ...
  // './app/views/settings.js' needs to be added
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify trailer tire positions and update SW cache</name>
  <files>app/views/unit-detail.js, sw.js</files>
  <action>
    **MAINT-11 — Trailer tire positions:**
    Read `app/views/unit-detail.js` and find the `renderTireMonitor` function. Per RESEARCH.md (Pitfall 7), TIRE_POSITIONS already has trailer-1-l/r and trailer-2-l/r, and renderTireMonitor already handles the isTrailer branch. Verify:
    1. The isTrailer check correctly identifies Trailer (and Reefer if applicable) unit types
    2. Trailer units show "Axle 1" and "Axle 2" groups (not "Steer" and "Drive")
    3. Trailer positions are trailer-1-l, trailer-1-r, trailer-2-l, trailer-2-r (4 positions, 2 axles x 2 sides)

    If the tire monitor already handles trailer positions correctly, no code change needed for MAINT-11.

    If any adjustment is needed (e.g., the isTrailer check is missing or wrong), make the minimal fix. Do NOT add new tire positions beyond the existing 4 trailer positions — the CONTEXT.md says "same UI as truck tire monitor, trailer axle positions."

    **MAINT-12 — Service worker cache update:**
    In `sw.js`:
    1. Bump the CACHE version: `'camiora-v51'` -> `'camiora-v52'`
    2. Add `'./app/views/settings.js'` to the STATIC array (place it after `'./app/views/unit-detail.js'` for consistent alphabetical ordering)
  </action>
  <verify>
    <automated>node -e "const sw = require('fs').readFileSync('sw.js','utf8'); if (!sw.includes('settings.js')) throw new Error('settings.js not in SW'); if (sw.includes('camiora-v51')) throw new Error('cache not bumped'); console.log('SW cache OK');"</automated>
  </verify>
  <done>SW cache bumped to v52 with settings.js included. Trailer tire positions verified (working or fixed if needed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of all Phase 10 features</name>
  <action>Human verifies the complete Phase 10 feature set in browser.</action>
  <what-built>
    Complete Phase 10 — Modular Maintenance Tracking:
    1. Settings page (#settings) with per-type milestone CRUD and due-soon threshold editing
    2. Dashboard cards with collapse/expand (OK items hidden by default)
    3. Milestones sorted by urgency within cards
    4. Configurable due-soon thresholds (replaced hardcoded 500 miles)
    5. Time-based due-soon detection added
    6. Coming Due filter tab on dashboard
    7. Gear icon in dashboard header linking to settings
    8. Trailer tire positions in unit detail
    9. Service worker cache updated
  </what-built>
  <how-to-verify>
    1. Open the app at the dashboard (#dashboard)
    2. Verify the gear icon appears in the dashboard header — tap it
    3. Settings page should show equipment types (Truck, Trailer) with milestones listed
    4. Try adding a new milestone to Truck (e.g., "Coolant Flush", 150000 mi)
    5. Verify it appears in the list — try editing its interval — try deleting it
    6. Change due-soon thresholds (e.g., 5000 miles, 30 days) and save
    7. Go back to dashboard — verify cards show collapse/expand for OK milestones
    8. Verify overdue items appear first within cards, due-soon second
    9. Check the "Coming Due" tab — should show units grouped by milestone type
    10. Navigate to a Trailer unit detail page — verify tire monitor shows Axle groups (not Steer/Drive)
    11. Verify due-soon counts in summary bar reflect the new thresholds
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- SW cache includes settings.js and version is bumped
- Full browser walkthrough confirms all 9 features listed above
</verification>

<success_criteria>
- Trailer tire monitor renders correctly for Trailer units
- SW cache version bumped with settings.js included
- User confirms all Phase 10 features work in browser
</success_criteria>

<output>
After completion, create `.planning/phases/10-modular-maintenance-tracking/10-04-SUMMARY.md`
</output>

name = "camiora-api-proxy"
main = "api-proxy.js"
compatibility_date = "2024-01-01"

[vars]
ALLOWED_ORIGIN = "https://alexubt.github.io"

# Set API keys as secrets (never in source):
#   cd worker && wrangler secret put SAMSARA_API_KEY
#   cd worker && wrangler secret put ANTHROPIC_API_KEY
